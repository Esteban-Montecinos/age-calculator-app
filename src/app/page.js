import HomeClient from "./client";

export default function Home() {
  return (
    <main className="flex min-h-screen flex-col items-center justify-center w-full m-auto">
      <HomeClient />
    </main>
  );
}
